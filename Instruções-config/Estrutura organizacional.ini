
├config
   ├───config
   │   └─── database.php          # Arquivo de configuração da conexão com o banco de dados.
   ├───controllers
   │   └─── controller.php        # Controlador principal para ações de logout.
   │   └─── GoogleAuthController.php # Controlador para autenticação com o Google Sign-In.
   │   └─── UserController.php
   |   └─── AuthController.php
       └─── profile.php 
   ├───folhas
   │   └─── style.css             # Arquivo de estilos CSS customizados.
   ├───imagens
   │   ├─── logo.svg              # Logotipo do projeto.
   │   └─── perfil.svg            # Imagem de perfil padrão.
   │       └───uploads          # Diretório para armazenar uploads de imagens de perfil (se utilizado um caminho no sistema de arquivos).
   ├───Instruções-config
   │   └─── arquivo mysqldump    # (Conteúdo deste diretório não detalhado nos códigos fornecidos).
   ├───public
   │   └─── js
   │       └─── profile.js  
            └─── login.js 
            └─── register.js     # Arquivo JavaScript para funcionalidades da página de perfil (incluindo upload de foto).
   ├───tviews                     # (Conteúdo deste diretório não detalhado nos códigos fornecidos).
   └───vendor
     └─── autoload.php          # Autoloader do Composer para carregar as dependências.
     └─── google                # Diretório contendo a biblioteca do Google API Client.
         └─── ...
    └───views
    └─── atualizar.php       # Página para atualizar o perfil do usuário (incluindo upload de foto).
    └─── login.html          # Página de login do usuário.